# This file marks the src/ folder as a Python package.
# It must exist for imports like:
#   from src.preprocess import load_and_preprocess
# to work correctly. Leave it empty.


""" The file itself is completely empty in terms of functionality — its mere presence in the src/ folder is what matters. 
When Python sees __init__.py inside a folder, it treats that folder as a "package" (a collection of importable modules). """